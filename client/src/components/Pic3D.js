import React from "react";

// Load the 3D panorama after giving Exif data
const Pic3D = () => {
  const pic = <iframe>Something</iframe>;

  return <div className="pic3d_div">{pic}</div>;
};

export default Pic3D;
